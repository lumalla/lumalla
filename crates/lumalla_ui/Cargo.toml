[package]
name = "lumalla_ui"
version = "0.1.0"
edition = "2024"
description = "Form UI protocol types for Lumalla config and lumalla-ui"
license = "MIT OR Apache-2.0"
repository = "https://github.com/lumalla/lumalla"
include = ["src/**/*", "LICENSE-*"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
